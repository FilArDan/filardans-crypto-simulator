# Скопируй этот файл в .env и заполни значения

# Режим запуска: 'production' активирует SameSite=None; Secure на куках
# Обязательно для работы в iframe (Foundry + ngrok)
NODE_ENV=production

# Секрет для подписи сессий — смени на любую длинную случайную строку
SECRET=замени-на-свой-секрет-здесь
